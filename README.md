# LLM-CompanyExtractor
Using LLM to extract company names and its domain mentioned in an article on website.

## To Run
* run pip install -r requirements.txt
* add OPENAI_API_KEY to env
* run main.py file
